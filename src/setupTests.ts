
import '@testing-library/jest-dom';
import { beforeEach, afterEach, vi } from 'vitest';

// Set up any global test environment configurations here
beforeEach(() => {
  // Setup before each test if needed
});

afterEach(() => {
  // Clean up after each test if needed
  vi.clearAllMocks();
});
